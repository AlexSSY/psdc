<% pizza_custom = PizzaCustom.includes(:pizza_tops)
    .find(cart_item.cart_itemable_id) %>

<%= turbo_frame_tag dom_id(cart_item) do %>
    <div class="flex items-center justify-between space-x-3 p-3">
        <div class="grow flex items-center space-x-2">
            <%= image_tag pizza_custom.constructor_image.variant(:thumb),
                class: "object-fill" %>
            <div class="flex grow flex-col">
                <div class="flex items-baseline gap-3">
                    <%= tag.span pizza_custom.name %>
                    <span class="text-red-500 text-xs border bg-red-100 border-red-500 rounded px-1 leading-4">custom</span>
                </div>
                <div class="flex space-x-2">
                    <%= link_to "Edit", "#", class: "text-red-500 underline text-xs" %>
                    <% if pizza_custom.pizza_crust.present? %>
                        <%= tag.span pizza_custom.pizza_crust&.name,
                            class: "text-xs text-red-600" %>
                    <% end %>
                    <%= tag.span pizza_custom.pizza_dough.name,
                        class: "text-xs text-sky-300" %>
                    <%= tag.span pizza_custom.pizza_size.name,
                        class: "text-xs" %>
                </div>
                <div class="flex space-x-2 items-baseline">
                    <%= link_to "Replace", "#", class: "text-xs text-sky-500 hover:underline" %>
                    <span class="text-xs text-zinc-500">
                        <%= pizza_custom.pizza_tops.pluck(:name).join(", ") %>
                    </span>
                </div>+-
                
                <div class="flex mt-1 items-center justify-between">
                    <%= component "cart_items/shared/controls",
                        cart_itemable_type: cart_item.cart_itemable_type,
                        cart_itemable_id: cart_item.cart_itemable_id,
                        quantity: cart_item.quantity %>
                    <span class="font-semibold"><%= cart_item.total_price %> UAH</span>
                </div>
            </div>
        </div>
    </div>
<% end %>