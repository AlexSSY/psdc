<div class="h-16 bg-black">
    <div class="container mx-auto">
        <div class="flex h-16 justify-between items-center">

            <%# Logo / Brand %>
            <%= render LogoBrandComponent.new %>

            <%# Centered header menu %>
            <%= render HeaderMenuComponent.new(current_path: request.path) %>
            
            <%# Cart & Authentication %>
            <%= render HeaderCartComponent.new %>

        </div>
    </div>
</div>
